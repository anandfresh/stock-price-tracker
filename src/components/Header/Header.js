import React from 'react';

import styles from './Header.module.scss';

const Header = () => {
  return (
    <header className={styles.Header}>
      <h2>Stock Tracking App </h2>
    </header>
  );
};

export default Header;
